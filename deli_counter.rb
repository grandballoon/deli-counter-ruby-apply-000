# Write your code here.
katz_deli = []


def line(array)
  if array.length == 0
    puts "The line is currently empty."
  else
    array.each do |person|
     spot = person.index += 1
    end
    puts "The line is currently:" + spot + person
  end
end
